node_modules/
dist/
.wrangler/

# Generated by `wrangler types` (recreated on every build) — not committed.
worker-configuration.d.ts

# Local secrets — never commit
.dev.vars
.env
.env.*

# Per-machine Claude Code overrides — never commit (the shared allow-list IS committed)
.claude/settings.local.json

*.log
.DS_Store
